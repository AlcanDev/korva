// Package integrations holds anti-drift tests that verify every IDE
// integration manifest under integrations/* references the universal
// BEHAVIOR.md guidelines at repo root.
//
// There is no runtime code — this package exists solely so the tests
// can run as part of the standard `go test ./...` invocation.
package integrations
