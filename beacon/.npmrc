# Explicit public npm registry. Korva is open source — no corporate
# mirrors are used. If you're behind a corporate proxy, override locally
# in ~/.npmrc rather than committing it to the repo.
registry=https://registry.npmjs.org/
audit=false
fund=false
