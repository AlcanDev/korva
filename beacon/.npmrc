# Force the public npm registry for this project.
# This ensures CI (GitHub Actions) and any developer — regardless of whether
# their machine has a corporate npm mirror in ~/.npmrc — always installs from
# the official registry. Override locally only if you know what you're doing.
registry=https://registry.npmjs.org/
audit=false
fund=false
