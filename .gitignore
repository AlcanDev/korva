# Binarios Go
korva
korva-vault
korva-sentinel
dist/
*.exe
# Stray binaries produced by `go run` / `go build` at repo root
/internal/hive-mock
/licensing-mock
/pubkey.pem

# Secretos y datos privados (NUNCA comitear)
*.key
admin.key
korva.config.local.json
# Production RSA private key — generated by forge/licensing-server/keygen
# NEVER commit. Deploy via KORVA_LICENSING_PRIVATE_KEY_PEM env var.
priv.pem

# Licensing infrastructure lives in korva-cloud (private repo) — never commit here
forge/licensing-server/
forge/licensing-mock/

# Datos locales del runtime (~/.korva/ no pertenece aquí)
.korva/

# Scrolls privados del equipo
lore/private/

# Base de datos local
*.db
*.db-wal
*.db-shm

# Go
*.test
*.out
vendor/

# Node / Beacon
node_modules/
beacon/dist/
beacon/.env
beacon/.env.local
*.tsbuildinfo

# Beacon dist copy used for embedui build — generated by `make vault-full`
vault/internal/ui/dist/

# macOS
.DS_Store

# Windows
Thumbs.db
desktop.ini

# IDE
.vscode/settings.json
.idea/
*.swp
*.swo

# Claude Code project settings (local only)
.claude/

# Build artifacts
*.tar.gz
*.zip
